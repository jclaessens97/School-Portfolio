import { TestBed } from '@angular/core/testing';

import { MakeSuggestionService } from '../make-suggestion.service';

describe('MakeSuggestionService', () => {
  // beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: MakeSuggestionService = TestBed.get(MakeSuggestionService);
  //   expect(service).toBeTruthy();
  // });
});
